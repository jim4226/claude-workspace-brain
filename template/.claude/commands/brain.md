---
description: Show the current WORKSPACE_BRAIN.md and offer to edit a section
---

Read `WORKSPACE_BRAIN.md` at the project root, then:

1. Summarise it back to me in ~10 lines: current focus, top 3 active threads,
   most recent decision, any open questions. Note any sections that look stale
   (no edits in >2 weeks) or overfull (>50 lines).

2. If I asked to update a specific section in my arguments, use the **Edit**
   tool to apply that change in place — never rewrite the whole file. Preserve
   the section structure and the inline HTML comments.

3. If I didn't ask for an edit, ask which section I want to touch and what
   the change is.

Do **not** invent content. If a section is empty, say so rather than filling
it with plausible-looking text.
